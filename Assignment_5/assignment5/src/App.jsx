import React, {useState} from "react";import data from './data';import Header from "./Components/Header"import Single from "./Components/Single"import Landing from "./Components/Landing"import Footer from "./Components/Footer"function App() {  const [showSingle, showSingleSet] = useState(false);  const [singleData, singleDataUp] = useState(null);  const activeData = (showSingle) => {    console.log('showSingle', showSingle);    if (showSingle) {      singleDataUp(showSingle);      showSingleSet(true);    }  }  return (    <>      <Header/>      {showSingle ? <Single data={singleData}/> : <Landing data={data} active={activeData}/>}      <Footer/>    </>  )}export default App