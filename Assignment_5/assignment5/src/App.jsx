import React, {useState} from "react";import data from './data';import Header from "./Components/Header"import Single from "./Components/Single"import Landing from "./Components/Landing"import Footer from "./Components/Footer"import TodoApp from "./Components/TodoApp";function App() {  const [showSingle, showSingleSet] = useState(false);  const [singleData, singleDataUp] = useState(null);  const activeData = (showSingle) => {    if (showSingle) {      singleDataUp(showSingle);      showSingleSet(true);    }  }  const handleBackVal = (backData) => {    if(backData == null){      singleDataUp(null);      showSingleSet(false);    }  }  return (    <>      <Header/>      <TodoApp />      {showSingle ? <Single {...singleData} onBackEvent={handleBackVal}/> : <Landing data={data} active={activeData}/>}      <Footer/>    </>  )}export default App